import { PurpleBackgroundDirective } from './purple-background.directive';

describe('PurpleBackgroundDirective', () => {
  it('should create an instance', () => {
    const directive = new PurpleBackgroundDirective();
    expect(directive).toBeTruthy();
  });
});
