import { TimestampParserPipePipe } from './timestamp-parser-pipe.pipe';

describe('TimestampParserPipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimestampParserPipePipe();
    expect(pipe).toBeTruthy();
  });
});
